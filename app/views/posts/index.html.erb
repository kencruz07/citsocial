<div class='row'>
  <div class='col-xs-6 col-xs-offset-3'>
    <h1>
      <%= link_to current_user.name, current_user, :class => 'post-links' %>
    </h1>
  </div>
</div>

<%= render :partial => 'posts/form', :locals => { :title => 'Write', :post => @post } %>

<div class='row'>
  <div class='col-xs-6 col-xs-offset-3'>
    <h3>
      Timeline:
    </h3>
  </div>
</div>

<div id='post-index'>
  <%= render @posts.latest %>
</div>
