
.row
  .col-xs-6.col-xs-offset-3
    %h1= title

.row
  .col-xs-6.col-xs-offset-3
    %table.table
      %tr
        %th Name
        %th
        - users.each do |user|
          = render :partial => 'users/user', :locals => {:user => user}
